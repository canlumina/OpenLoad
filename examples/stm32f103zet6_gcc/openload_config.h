/*
 * OpenLoad 用户配置 shim — 真实配置由 Kconfig (.config) 管理.
 *
 * 编辑配置: python tools/menuconfig.py examples/stm32f103zet6_gcc/.config
 * 生成的真实 #define 在 build/<cfg>/kconfig/openload_autoconfig.h.
 *
 * 历史 #define 列表见 git history (commit 之前的版本).
 */
#pragma once

#include "openload_autoconfig.h"
