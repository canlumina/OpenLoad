# OpenOCD 配置 - STM32F103 工程
#
# 仅声明 interface + target. 具体动作 (program/erase/reset/halt) 由
# .vscode/tasks.json 在调用时通过 -c 传入, 保持本文件干净.
source [find interface/stlink.cfg]
source [find target/stm32f1x.cfg]
