# Build output —— 无前导斜杠, 匹配任意层级的 build/ (含 examples/*/build/)
build/

# Python 工具产物 (tools/*.py)
__pycache__/
*.pyc

# menuconfig (kconfiglib) 保存时生成的备份
*.config.old

# Claude Code
.claude/

# VS Code
.vscode/

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Firmware binaries (烧录测试用的 App / bootloader bin, 不进版本库)
*.bin

# 其它 AI 工具
GEMINI.md
